#include <string>
#include <algorithm>
#include <iostream>
#include<vector>
using namespace std;
/*
void PrintSpacesPositions(string& str) {
	// напишите реализацию
	std::string::iterator it = find(str.begin(),str.end(),' ');
	for (auto iter=it; iter != str.end();iter=find(iter+1,str.end(),' '))
	{
		cout << distance(str.begin(),iter) << endl;
	} 
}

int main() {
	string str = "He said: one and one and one is three"s;
	PrintSpacesPositions(str);
	return 0;
}
*/